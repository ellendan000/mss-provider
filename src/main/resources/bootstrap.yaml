server:
  port: 8081

spring:
  application:
    name: mss-provider
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8181/eureka
    fetch-registry: true
    register-with-eureka: true
